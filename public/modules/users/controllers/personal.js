'use strict';

angular.module('users').controller('PersonalController', ['$scope', '$http', '$location','$timeout','Authentication',
	function($scope, $http, $location,$timeout ,Authentication) {
		$scope.loading = false;
		// var id = Authentication.getHash(1);
		
	}	
]);
